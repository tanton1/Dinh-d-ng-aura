# These values are deliberately non-sensitive.  Vite reads them during the
# e2e build so browser tests exercise the app shell instead of Firebase Auth.
VITE_FORCE_DEMO=true
VITE_ENABLE_DEMO_OTP=true
